name: Threads 自動投稿（AIサポート）

on:
  schedule:
    - cron: '0 3 * * *'
    - cron: '0 8 * * *'
    - cron: '0 12 * * *'
    - cron: '0 21 * * *'
  workflow_dispatch:
    inputs:
      dry_run:
        description: '投稿しない（プレビューのみ）にする場合はtrueを選択'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 依存パッケージをインストール
        run: npm install

      - name: Threads に投稿
        run: node auto_post_aisupport.js
        env:
          THREADS_ACCESS_TOKEN: ${{ secrets.THREADS_ACCESS_TOKEN_AISUPPORT }}
          THREADS_USER_ID: ${{ secrets.THREADS_USER_ID_AISUPPORT }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
